__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# db.sqlite3, media/, and staticfiles/ are intentionally committed:
# the Vercel deploy bundles the seeded db + demo photos for a read-only
# browsing demo (see README's Deploying to Vercel section).
